import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Sign Up',
  description: 'Create your WAgenT account. Get an AI WhatsApp agent for your business in Ghana. 14-day free trial, no card required.',
}

export default function SignupLayout({ children }: { children: React.ReactNode }) {
  return children
}
